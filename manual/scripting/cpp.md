# C++

> [!NOTE]
> Stub. Should cover: module layout under `Source/Engine`, the `API_*` binding macros and what
> each generates, scripting object lifetime, and building a native plugin.

Reference: [C++ API](../../api-cpp/index.md).
