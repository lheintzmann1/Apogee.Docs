# C#

> [!NOTE]
> Stub. Should cover: the managed assembly layout, writing gameplay scripts, editor plugins and
> custom editors, and how the generated bindings relate to the C++ `API_*` declarations.

Reference: [C# API](../../api/index.md).
