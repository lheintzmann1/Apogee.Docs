# Audio

Apogee uses [FMOD](https://www.fmod.com/) for audio.

> [!NOTE]
> Stub. Should cover: the `AudioSource` and `AudioListener` actors, bank management through
> `AudioSettings.Banks`, events and parameters, and mixer buses.
