# Rendering

Apogee renders through Vulkan and DirectX 12.

> [!NOTE]
> Stub. Should cover: the render graph, materials and shaders, the shader asset pipeline
> (`Source/Shaders` compiled to `Content/Shaders` by `Apogee.Build`), and render passes.
